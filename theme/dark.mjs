import { createTheme } from "@nextui-org/react"

export const dark = createTheme({
  type: 'dark',
  theme: {
    colors: {
      background: '#0c121e',
      text: '#fff',
      dark: '#0c121e'
    },
    space: {},
    fonts: {}
  }
})